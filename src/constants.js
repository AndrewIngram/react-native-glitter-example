export const WIDTH = 1024;
export const HEIGHT = 1024;

export const PIXEL_SIZE = 2;

export const NUM_COLUMNS = Math.ceil(WIDTH / PIXEL_SIZE);
export const NUM_ROWS = Math.ceil(HEIGHT / PIXEL_SIZE);

export const NUM_PIXELS = NUM_COLUMNS * NUM_ROWS;